<?xml version="1.0" encoding="UTF-8"?>
<project name="config.common">
	<echo>[common.xml] Declare vars</echo>
	
	<!-- properties to use via command line -->
	<property name="mod.add-server" value="" />
	<property name="mod.add-js" value="" />
	<property name="mod.add-css" value="" />
	<property name="config.add" value="" />
	<property name="config.file" value="" />
	<property name="config.no-css" value="" />
	<property name="config.no-js" value="" />
	<property name="config.name" value="asp-net-mvc,${config.add}" />
	<property name="mod.html-type" value="xhtml1" />

	<!-- module data (per project) -->
	<property name="mod.css-list" value="reset.css,${mod.add-css}" />
	<property name="mod.js-list" value="**/jquery**,${mod.add-js}" />
	<property name="base.mod.server-list" value="core,${mod.add-server}" />
	<property name="mod.server-list" value="${base.mod.server-list}" />

	<!-- util paths -->
	<property name="dir.destination" value="${basedir}/destination" />
	<property name="dir.src" value="${basedir}/source" />
	<property name="dir.server" value="${dir.src}" />
	<property name="dir.front" value="${dir.src}/front-end" />
	<property name="dir.html" value="${dir.front}/${mod.html-type}" />
	<property name="dir.css" value="${dir.front}/css" />
	<property name="dir.js" value="${dir.front}/js" />
	<property name="dir.assets" value="${dir.destination}/assets" />
	<property name="dir.script-assets" value="${dir.assets}" />
	
	<!-- files -->
	<property name="file.property-name" value="ant-config.xml" />
	<property name="file.custom-config" value="${config.file}" />
	
	<!-- replaceList -->
	<property name="str.replace-list" value="" />
	
</project>

